/*
** EPITECH PROJECT, 2022
** image_opt
** File description:
** Source code to build image opt
*/
#include "build_scene_images.h"

int build_img_scale(entity_t *entity, char *value)
{
    printf("build image scale...\n");
    return (0);
}

int build_img_rotation(entity_t *entity, char *value)
{
    printf("build image rotation...\n");
    return (0);
}

int build_img_texture(entity_t *entity, char *value)
{
    printf("build image texture...\n");
    return (0);
}